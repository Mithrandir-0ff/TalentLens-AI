### Задание - Оценка качества ретривера

1. В предыдущем артефакте разработан план оценки, включающий выбор метрик, определение сценариев использования и тд.
2. Дополнены данные в `scout_knowledge.txt`.
3. Сформирован проверочный датасет `retriever_test_data.json` из 25 запросов с ручной разметкой «золотого стандарта».
4. В `knowledge_base.py` дополнен код для уникальной идентификации чанков и сохранения их ID в метаданных векторного хранилища.
5. Написан скрипт `eval_retriever.py`, который автоматизирует процесс запросов к векторному хранилищу FAISS и вычисляет средние метрики.